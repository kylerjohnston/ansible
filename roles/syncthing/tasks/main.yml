---
- name: Install syncthing
  package:
    name: syncthing
    state: latest
  become: yes

- name: Start and enable syncthing
  systemd:
    name: syncthing@{{ username }}.service
    state: started
    enabled: yes
    user: yes
  become: yes

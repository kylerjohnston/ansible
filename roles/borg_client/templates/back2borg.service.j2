[Unit]
Description=Back up home directory to remote Borg repository

[Service]
Type=forking
ExecStart=/home/{{ username }}/.local/bin/back2borg.sh {{ borg_share }}/{{ ansible_hostname }}

[Install]
WantedBy=default.target
